@model HomeViewModel
@using Challenge.Valkimia.Presentation

@inject IAuthenticatedUserService UserService
@{
    ViewBag.Title = @L[ResourceKeys.Labels_Home];
    ViewBag.pTitle = @L[ResourceKeys.Labels_Home];
    ViewBag.pageTitle = "Challenge Valkimia by Stefano Zucarelli";
}

<partial name="_DatatablesCss" />

<div class="row">
    <div class="col-xl-4">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title mb-4">Clientes</h4>

                <div class="row">
                    <div class="col-sm-8">
                         <p class="text-muted">Este mes</p>
                        <h3>1245</h3>
                        <p class="text-muted"><span class="text-success me-2">12%<i class="mdi mdi-arrow-up"></i></span>desde el período anterior</p>
                        <div class="mt-4" style="margin-bottom: 15px;">
                            <a class="btn btn-primary waves-effect waves-light btn-sm" asp-controller="Clientes" asp-action="Index">Gestionar clientes <i class="mdi mdi-arrow-right"></i></a>
                        </div>
                    </div>
                    <div class="col-sm-4">
                    </div>
                </div>

                <p class="text-muted mb-0">Gestione altas, bajas y modificiones de clientes.</p>
            </div>
        </div>
    </div>

    <div class="col-xl-8">

    </div>

</div>

